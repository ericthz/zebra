# AI Agent 架构设计要点

## 分层
1. Provider 层：屏蔽不同 LLM 厂商协议差异
2. 编排层：Agent 主循环，串起 LLM、工具、记忆、技能
3. 工具层：原子操作 + 权限 + 审计
4. 安全层：注入防护、内容审核、脱敏、审计

## 关键原则
- 依赖倒置：上层依赖接口，不依赖具体实现
- 上下文工程：token 预算内保留最有价值消息，超预算裁剪或摘要
- 安全边界：高危操作（写文件、执行命令）必须二次确认
- 可观测：结构化日志 + 指标 + 审计，全链路可追溯
